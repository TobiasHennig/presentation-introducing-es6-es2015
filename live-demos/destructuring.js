var person = {
  name: 'John',
  age: 25
};

// TODO
var name = person.name;
var age = person.age;

console.log(name, age);

// TODO
function sayName (person) {
  var name = person.name;
  console.log(name);
}

sayName(person);
